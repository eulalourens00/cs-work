<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ballon.HomePage"
             Title="Новогодний Шарик"
             BackgroundColor="#77c763">
    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20" BackgroundColor="#77c763">

            <Frame Style="{StaticResource ChristmasFrame}"  Margin="0,20,0,0">
                <VerticalStackLayout Spacing="10" HorizontalOptions="Center">
                    <Label Text="🎄 Новогодний Волшебник 🎄" 
                           Style="{StaticResource TitleLabel}"/>
                    <Label Text="Создай свой уникальный ёлочный шар!" 
                           Style="{StaticResource GlowingText}"/>
                </VerticalStackLayout>
            </Frame>

            <Frame BackgroundColor="Transparent" 
                   BorderColor="Transparent"
                   CornerRadius="10"
                   Padding="10"
                   HorizontalOptions="Center">
                <HorizontalStackLayout Spacing="15" HorizontalOptions="Center">
                    <Label Text="✨" FontSize="30"/>
                    <Label Text="✨" FontSize="30"/>
                    <Label Text="✨" FontSize="30"/>
                </HorizontalStackLayout>
            </Frame>

            <VerticalStackLayout Spacing="5" 
                                 HorizontalOptions="Center"
                                 Margin="0,10,0,0">
                <Grid ColumnDefinitions="auto, auto" 
                      HorizontalOptions="Center"
                      ColumnSpacing="5">
                    <Label Text="Новый год" 
                           FontSize="24"
                           FontAttributes="Bold"
                           TextColor="#e5f2e1"
                           Grid.Column="0"/>
                    <Label Text="все ближе" 
                           FontSize="24"
                           FontAttributes="Bold"
                           TextColor="#e5f2e1"
                           Grid.Column="1"/>
                </Grid>

                <Label Text="Пришло время украшать ёлку!" 
                       FontSize="16"
                       TextColor="#31472b"
                       HorizontalOptions="Center"/>
            </VerticalStackLayout>
 
            <Frame Style="{StaticResource CreateButtonFrame}" 
                   Margin="0,30,0,0" BackgroundColor="#d6883e">
                <Frame.GestureRecognizers>
                    <TapGestureRecognizer Tapped="TapGestureRecognizer_Tapped"/>
                </Frame.GestureRecognizers>

                <VerticalStackLayout Spacing="10" 
                                     HorizontalOptions="Center">
                    <Label Text="🎨" 
                           FontSize="40"
                           HorizontalOptions="Center"/>

                    <Label Text="Создать шар" 
                           Style="{StaticResource CreateButtonLabel}"/>
                </VerticalStackLayout>
            </Frame>
            <Label Text="Счастливого Нового Года!" 
                    FontSize="32"
                    TextColor="white"
                    HorizontalOptions="Center"/>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>